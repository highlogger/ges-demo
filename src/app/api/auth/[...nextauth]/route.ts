// ============================================================
// GES SOLAR PRO — Auth.js v5 API Route
// ============================================================
import { handlers } from '@/lib/auth';

export const { GET, POST } = handlers;
